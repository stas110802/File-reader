<Window x:Class="PngReader.XAML.PngView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PngReader.XAML"
        mc:Ignorable="d"
        Title="PngView" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="InfosDG" IsReadOnly="True" HorizontalAlignment="Left" VerticalAlignment="Top" Width="531" Height="350">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="60" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Name chank" Width="150" Binding="{Binding NameChank}"/>
                <DataGridTextColumn Header="Length" Width="120" Binding="{Binding Length}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
